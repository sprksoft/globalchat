services:
  smppgc:
    build:
      dockerfile: Dockerfile
    networks:
      - ldeveuorg_backend_net
      - gc
    volumes:
      - proffilter:/var/proffilter
      - auth:/var/auth

    expose:
      - 8080

volumes:
  proffilter:
  auth:

networks:
  gc:
  ldeveuorg_backend_net:
    external: true
