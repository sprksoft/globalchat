<!-- vim: set ft=html: -->
<!DOCTYPE html>
<html>
  <head>
    <title>{{#if title}}{{title}} - {{/if}}Global Chat {{#if debug}}(debug){{/if}}</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1,interactive-widget=resizes-content">
    <link rel="icon" href="{{root_url}}/static/logo.webp">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{root_url}}/static/v1.css?ckey={{version}}" />

    {{#> head}}
    {{/head}}
    <style type="text/css">
      {{theme_css}}
    </style>


    {{#if offline}}
    {{else}}
    <script type="text/javascript">
      var local_commands = [];
      {{#if skip_login}}
      const SKIP_LOGIN=true;
      {{else}}
      const SKIP_LOGIN=false;
      {{/if}}

      {{#if debug}}
      function reload_js() {
        location = "/debug/reload_js"
      }
      local_commands.push(["/reloadjs", function(){
        reload_js();
      }]);
      {{/if}}

      let protocol = "ws://"
      if (location.protocol.endsWith("s:")){
        protocol =  "wss://"
      }
      const VERSION="{{version}}";
      const MAX_SERIALS_PER_MONTH = 20;
      let [year, month, serial] = VERSION.split(".").map((value)=>parseInt(value));
      const VERSION_INT = ((year - 2024) * (12 * MAX_SERIALS_PER_MONTH) + month * MAX_SERIALS_PER_MONTH + serial);


      const WEBSOCKET_URL=protocol+location.host+"{{root_url}}/socket/v1";
      const ROOT_URL="{{root_url}}";
      const MAX_MESSAGE_LEN="{{max_message_len}}";
    </script>
    <script defer type="text/javascript" src="{{root_url}}/static/v1.js?ckey={{version}}"></script>
    {{/if}}
  </head>
  <body>
    {{> login_popup}}
    {{> chat}}
  </body>
</html>
